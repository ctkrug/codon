// Entry point for the Codon workspace UI. The build epics wire the sequence
// input, six-frame viewer, ORF highlighter, and stat panels in here; for now
// this confirms the module graph loads cleanly in a real browser.
console.log("Codon workspace ready");
